<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    SizeToContent="Height"
    FontSize="40"
    Topmost="True">
    <Grid Margin="10,40,10,10">
        <Grid.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="FontFamily" Value="CaskaydiaCove Nerd Font Mono" />
            </Style>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Column="1" Margin="10" TextAlignment="Center" FontSize="50">
            <TextBlock.Effect>
                <DropShadowEffect Direction="-45" Color="#FFB6C2CB" ShadowDepth="10" BlurRadius="14" />
            </TextBlock.Effect>
            About me
        </TextBlock>
        <Image Grid.Column="1" Source="Media/PSConfEU-Logo.png" HorizontalAlignment="Right" Height="100"/>
        <Image Grid.Column="0" Grid.Row="0" Grid.RowSpan="3" Source="Media/About_Me.png">
            <Image.Effect>
                <DropShadowEffect Direction="-45" Color="#FFB6C2CB" ShadowDepth="10" BlurRadius="14" />
            </Image.Effect>
        </Image>
        <StackPanel Grid.Column="1" Grid.Row="1" VerticalAlignment="Center" Margin="20,20,20,20">
            <StackPanel.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="TextBlock.Effect">
                        <Setter.Value>
                            <DropShadowEffect Direction="-45" Color="#FFB6C2CB" ShadowDepth="10" BlurRadius="14" />
                        </Setter.Value>
                    </Setter>
                    <Setter Property="FontSize" Value="35" />
                    <Setter Property="FontFamily" Value="CaskaydiaCove Nerd Font Mono" />
                </Style>
            </StackPanel.Resources>
            <TextBlock>@{</TextBlock>
            <TextBlock xml:space="preserve"><Run Foreground="Black" Text="    First   = "/><Run Foreground="#FF8B0000" Text="'Bartosz'"/></TextBlock>
            <TextBlock xml:space="preserve"><Run Foreground="Black" Text="    Last    = "/><Run Foreground="#FF8B0000" Text="'Bielawski'"/></TextBlock>
            <TextBlock xml:space="preserve"><Run Foreground="Black" Text="    Twitter = "/><Run Foreground="#FF8B0000" Text="'@bielawb'"/></TextBlock>
            <TextBlock xml:space="preserve"><Run Foreground="Black" Text="    Email   = "/><Run Foreground="#FF8B0000" Text="'bartek.bielawski@live.com'"/></TextBlock>
            <TextBlock xml:space="preserve"><Run Foreground="Black" Text="    Work    = "/><Run Foreground="#FF8B0000" Text="'Optiver'"/></TextBlock>
            <TextBlock xml:space="preserve"><Run Foreground="Black" Text="    Git     = "/><Run Foreground="#FF8B0000" Text="'github.com/bielawb'"/></TextBlock>
            <TextBlock>}</TextBlock>
        </StackPanel>
        <TextBlock Grid.Column="1" Grid.Row="2" FontSize="30" TextAlignment="Left" Margin="30,30,30,30">
            <TextBlock.Effect>
                <DropShadowEffect Direction="-45" Color="#FFB6C2CB" ShadowDepth="10" BlurRadius="14" />
            </TextBlock.Effect>
            Forgotten Gems of PowerShell
        </TextBlock>
        <Image Grid.Column="1" Grid.Row="2" Source="Media/Wiesbaden.png" HorizontalAlignment="Right" Height="100" />
    </Grid>
</Window>